# 2026-07-06 对话记录

## 1. 用户要求读取本地 Markdown 并总结任务

- 用户：要求 AI 读取本地下所有 Markdown，并说明 Markdown 中当前要求 AI 做什么任务、有哪些要求；同时说明已创建 git 仓库，方便 AI 随时回退。
- AI：
  - 枚举了仓库内 Markdown 文件，排除 `.git/`，共发现 111 个 Markdown。
  - 重点读取了根目录 `spec.md`、`README.md`、`memory/`、`.github/` 规则文件、CPU 接口文档等项目级 Markdown。
  - 通过全文检索核对第三方依赖文档未提出新的当前任务要求。
  - 发现当前任务聚焦 RV32E_Zicsr、NEMU、NPC/Verilog 五级流水线、AXI、RT-Thread、ysyxSoC/Verilator CI，而不是旧 RV64 Linux/Ubuntu bring-up。
  - 发现 `spec.md` 第 11 行以“直到跑完soc环境下的”结尾，语义未完成，后续不应自行补全。

## 2. 用户要求先完成第一个步骤

- 用户：要求 AI 自行决定需要进行什么工作，先完成“完成 NEMU，跑完 RISC-V 架构的 `am-kernels` 中的 `cpu-test` 功能”。
- AI：
  - 检查了 `ysyx-workbench/nemu`、`abstract-machine`、`am-kernels/tests/cpu-tests` 的构建入口和现有 RV32 解释器。
  - 发现环境变量 `NEMU_HOME`、`AM_HOME` 指向旧目录，因此后续构建命令显式传入本仓库路径。
  - 修正 AM/NEMU 构建配置和 batch 运行方式。
  - 补齐 NEMU RV32I/RV32M 指令解释器和 KLIB 基础字符串/格式化函数。
  - 最终运行 RISC-V `cpu-tests`，35 项全部 PASS。

## 3. 用户要求完成第二个步骤并纠正 RT-Thread 裸机边界

- 用户：
  - 要求完成“完成 NEMU，跑完 RISC-V 架构的 RT-Thread”，说明 RT-Thread 放在 `~/Templates`。
  - 后续纠正 AI 的表述：RT-Thread 是要跑在 NEMU 上的裸机 guest，不能使用标准 C 头包装或宿主 libc/newlib，要补对应的裸机程序和 guest 侧软件。
- AI：
  - 确认纠正：后续按 freestanding guest 侧头文件和实现处理，不引入宿主 C 库。
  - 复现 RT-Thread 构建最初因 `<stdio.h>` 落到 `/usr/riscv64-linux-gnu/include` 而失败的问题。
  - 在 AM/KLIB 中补充 RT-Thread 构建需要的裸机 C/POSIX 头和函数实现。
  - 在 NEMU 中补充 RISC-V CSR、`ecall/mret` 和机器模式异常最小支持。
  - 修正 AM RISC-V trap/context 布局，并在 RT-Thread AM BSP 中实现线程上下文切换。
  - 为 RT-Thread BSP 增加 `am_exit` 自动退出命令，使默认运行可以以 `HIT GOOD TRAP` 结束。
  - 最终用默认命令跑通 RT-Thread，自动执行 shell 命令后 `HIT GOOD TRAP`。
  - 回归 RISC-V `cpu-tests`，35 项全部 PASS。

## 4. 用户要求实现 NPC 五级流水线并接 NEMU difftest

- 用户：
  - 要求重新读取工作区下所有 Markdown。
  - 说明 NEMU 除 difftest 外已经完成，现在要实现 NPC 五级流水线。
  - 明确本阶段不要 icache/dcache，不要分支预测，不要 forwarding/前递。
  - 要求用 Verilator 将 Verilog 编译成高性能 C/C++ 仿真环境，并放到类似 NEMU 的仿真环境运行。
  - 要求接上 NEMU difftest，因为 NEMU 已能跑 RT-Thread，可认为 NEMU 正确。
  - 要求 NPC 每执行一条指令与 NEMU 比较一次，比较当前 PC、32 个寄存器和 CSR。
  - 中间任务：先跑 `am-kernels/tests/cpu-tests` 不报错。
  - 要求按此前读取的 Markdown 约束执行。
- AI：
  - 重新枚举工作区 Markdown，排除 `.git/` 后发现 126 个 Markdown；通过关键词扫描确认当前任务相关约束主要来自 `spec.md`、`.github/AGENTS.md`、RTL 生成流程、`memory/` 和 CPU 接口文档，rocket-chip/chisel 依赖文档未提出新的当前任务要求。
  - 明确本阶段按用户最新要求覆盖长期 RV32E/icache/AXI 规格：先实现 RV32 + 32 GPR、无 cache/预测/前递、Verilator + difftest + cpu-tests。
  - 在落 RTL 前给出需求、协议规则、状态机、不变量、数据通路约束和 RTL 拓扑推导。
  - 新增 NPC 五级流水线 RTL、Verilator C++ 仿真环境、AM `riscv32-npc` 入口和 NEMU reference difftest 导出接口。
  - 修正 `platform/npc.mk`，避免旧环境变量 `NPC_HOME` 指向 `/home/ywj/fuwuqi_back/...`。
  - 将 AM/NPC 裸机 `halt()` 改成 `a0 + ebreak`，保证 guest 以裸机 trap 方式退出。
  - 验证 `riscv32-npc` 的 `dummy`、覆盖性单测和全量 `cpu-tests`，最终 35 项全部 PASS。
  - 回归 `riscv32-nemu cpu-tests`，35 项全部 PASS。
  - 追加修正 NEMU reference `.so` 的增量重建链路和 0 长度 difftest memcpy 防护，并复跑 `riscv32-npc ALL=dummy` PASS。
