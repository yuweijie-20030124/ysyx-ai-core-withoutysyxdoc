# 2026-07-05 对话记录

本文件记录用户和 AI agent 在本仓库中的对话。后续对话应继续追加到
`memory/whataitalkwithman/` 下的 Markdown 文件中。

## 1. 用户要求查看 memory

- 用户：先了解下我在 memory 中放的需要注意的东西，然后告诉我里面放了什么。
- AI：检查了 `memory/` 目录，读取了 `tools.md`、`RISCV-32E Core AI生成规格文档.md`、
  `whataido/requirement.md`，并确认 `spec/riscv-unprivileged.pdf` 与
  `oss-cad-suite-linux-x64-20260705.tgz` 的元信息。
- AI 回复摘要：
  - `tools.md` 记录 gtkwave、verilator、riscv64-linux-gnu-、yosys-sta 等工具。
  - RV32E 规格文档记录 RV32E_Zicsr、仅 M 模式、单核、默认复位地址 `0x20000000`、
    CSR/异常/访存/icache/AXI4/RT-Thread CI 目标等。
  - `whataido/requirement.md` 当时基本为空。
  - `whataitalkwithman` 当时为空目录。

## 2. 用户要求清理新增 `.github`

- 用户：说明新增 `.github` 来源于过去“制作 RV64 RTL 来启动 Linux”的工程；现在的规格只是生成
  C 语言 NEMU CPU 模拟器和 Verilog 五级流水线，具体看
  `/home/ywj/ysyx_ai_core/memory/RISCV-32E Core AI生成规格文档.md`；
  要求判断哪些不需要并删除。
- AI：最初准备删除整个 `.github`，用户打断并说“你别把整个都删了”。
- AI：改为只删除旧 RV64/Linux/Ubuntu bring-up 和旧 agent 证据体系，保留 `.github` 作为轻量入口。
- AI 回复摘要：
  - 删除旧 `task-runs`、`db-backup`、`e2e`、旧 `.github/memory`、旧 agents、workflows、
    runtime artifacts 等。
  - 重写 `.github/AGENTS.md`、`.github/copilot-instructions.md`。
  - 新增 `.github/instructions/rtl-generation-workflow.instructions.md`。
  - 复查 `.github` 从约 287M 降到约 20K，只剩 3 个文件。

## 3. 用户再次提出同一清理请求

- 用户：再次说明新增 `.github` 是旧 RV64 Linux 工程，现在只要 C 语言 NEMU 模拟器和 Verilog 五级流水线，
  要求删除不需要的内容。
- AI：重新检查当前磁盘状态，发现完整旧版 `.github` 又存在，约 287M。
- AI：再次删除旧 RV64/Linux/Ubuntu/agent evidence 大块，保留并重写轻量 `.github` 入口。
- AI 回复摘要：
  - 当前保留 `.github/AGENTS.md`、`.github/copilot-instructions.md`、
    `.github/instructions/rtl-generation-workflow.instructions.md`。
  - 剩余旧目标关键词只在“不要使用旧目标”的说明中出现。
  - 没有修改 NEMU/RTL 源码，所以未跑功能测试。

## 4. 用户要求固化修改记录和对话记录

- 用户：要求“你做的任何一切修改都要放在
  `/home/ywj/ysyx_ai_core/memory/whataido` 目录中，
  你和我说的一切对话都要放在
  `/home/ywj/ysyx_ai_core/memory/whataitalkwithman` 中，请固化下来，放到本地 markdown 中”。
- AI 当前动作：
  - 正在把该要求写入 `memory/whataido/requirement.md`。
  - 正在新增 `memory/whataido/2026-07-05.md` 记录已做修改。
  - 正在新增本文件记录对话。
  - 正在把该要求写入 `.github` 入口，方便后续 agent 遵守。
