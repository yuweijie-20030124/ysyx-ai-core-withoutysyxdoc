# 2026-07-05 AI 修改记录

## 1. 清理旧 `.github` RV64 Linux/Ubuntu 遗留

- 时间：2026-07-05 约 23:20 CST 前。
- 修改范围：
  - 删除旧 `.github/task-runs/`
  - 删除旧 `.github/db-backup/`
  - 删除旧 `.github/e2e/`
  - 删除旧 `.github/ai-env/`
  - 删除旧 `.github/archive/`
  - 删除旧 `.github/runtime-artifacts/`
  - 删除旧 `.github/skills/`
  - 删除旧 `.github/workflows/`
  - 删除旧 `.github/agents/`
  - 删除旧 `.github/instructions/`
  - 删除旧 `.github/memory/`
  - 删除旧 `.github/agent-env-*.json`
  - 删除旧 `.github/agentic-hardware-blueprint.md`
  - 重写 `.github/AGENTS.md`
  - 重写 `.github/copilot-instructions.md`
  - 新增 `.github/instructions/rtl-generation-workflow.instructions.md`
- 原因：用户说明新增 `.github` 来自过去“RV64 RTL 启动 Linux”的工程，而当前规格只需要
  C 语言 NEMU 风格 RV32E_Zicsr CPU 模拟器和 Verilog 五级流水线 CPU。
- 修改内容摘要：
  - 移除旧 RV64/Linux/Ubuntu/OpenSBI/rootfs/virtio/systemd/agent e2e 证据库和调度体系。
  - 保留 `.github` 目录作为轻量规则入口。
  - 新入口明确当前不是 RV64 Linux/Ubuntu bring-up，而是 RV32E_Zicsr、NEMU C 模拟器、
    Verilog 五级流水线、RT-Thread/CI。
  - RTL 流程文件保留“写 RTL 前先梳理需求、协议、状态机、不变量、数据通路、拓扑”的约束。
- 验证：
  - `find .github -maxdepth 4 -type f` 显示只剩 3 个文件。
  - `du -sh .github` 显示约 20K。
  - `rg` 检查 RV64/Linux/Ubuntu 等关键词，剩余命中只在“旧目标边界/禁止沿用”的说明中；
    `riscv64-linux-gnu-` 是 `memory/tools.md` 已给出的工具链前缀。
- 功能测试：未运行。原因是本次只清理和重写 `.github` 文档规则，没有修改 NEMU 或 RTL 源码。

## 2. 固化修改记录和对话记录规则

- 时间：2026-07-05 23:20 CST。
- 修改范围：
  - 重写 `memory/whataido/requirement.md`
  - 新增 `memory/whataido/2026-07-05.md`
  - 新增 `memory/whataitalkwithman/2026-07-05.md`
  - 更新 `.github/AGENTS.md`
  - 更新 `.github/copilot-instructions.md`
- 原因：用户要求“你做的任何一切修改都要放在
  `/home/ywj/ysyx_ai_core/memory/whataido` 中，你和我说的一切对话都要放在
  `/home/ywj/ysyx_ai_core/memory/whataitalkwithman` 中，请固化下来，放到本地 markdown 中”。
- 修改内容摘要：
  - 在 `whataido/requirement.md` 中写明以后所有 AI 修改都要在该目录记录。
  - 在 `whataitalkwithman/2026-07-05.md` 中记录当前可见对话。
  - 在 `.github` 的 agent 入口中加入记忆记录要求，方便后续 agent 自动遵守。
- 验证：
  - 待本次补丁后复查文件存在性和关键词。
- 功能测试：不适用。本次是文档和流程固化。
